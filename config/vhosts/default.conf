<VirtualHost *:80>
    ServerName localhost
    Redirect permanent / https://localhost/
</VirtualHost>

<VirtualHost *:80>
    ServerName test.dev.local
    Redirect permanent / https://test.dev.local/
</VirtualHost>

<VirtualHost *:80>
    ServerName foerderverein.dev.local
    Redirect permanent / https://foerderverein.dev.local/
</VirtualHost>

<VirtualHost *:80>
    ServerName vatra.dev.local
    Redirect permanent / https://vatra.dev.local/
</VirtualHost>

<VirtualHost *:80>
    ServerName voda.dev.local
    Redirect permanent / https://voda.dev.local/
</VirtualHost>

<VirtualHost *:80>
    ServerName kamen.dev.local
    Redirect permanent / https://kamen.dev.local/
</VirtualHost>

# Allows HTTPS on localhost. You will need to use mkcert on your local machine
# to create the `cert.pem` and `cert-key.pem` files, and then place them in the
# `./config/ssl` directory. You could also create certificates for any local
# testing domain you wish such as `localapp.test`, you will then edit your hosts
# file to map that domain name to 127.0.0.1 and then configure your vhosts below
# accordingly:
#
# <VirtualHost *:443>
#     ServerAdmin webmaster@localhost
#     DocumentRoot ${APACHE_DOCUMENT_ROOT}
#     ServerName localhost
# 	<Directory ${APACHE_DOCUMENT_ROOT}>
# 		AllowOverride all
# 	</Directory>

#     SSLEngine on
#     SSLCertificateFile /etc/apache2/ssl/cert.pem
#     SSLCertificateKeyFile /etc/apache2/ssl/cert-key.pem
# </VirtualHost>

<VirtualHost *:443>
    ServerAdmin robert@samarija.com
    DocumentRoot ${APACHE_DOCUMENT_ROOT}/local/public
    ServerName localhost
	<Directory ${APACHE_DOCUMENT_ROOT}/local/public>
		AllowOverride all
	</Directory>

    SSLEngine on
    SSLCertificateFile /etc/apache2/ssl/cert.pem
    SSLCertificateKeyFile /etc/apache2/ssl/cert-key.pem
</VirtualHost>

<VirtualHost *:443>
    ServerAdmin robert@samarija.com
    DocumentRoot ${APACHE_DOCUMENT_ROOT}/test/public
    ServerName test.dev.local
	<Directory ${APACHE_DOCUMENT_ROOT}/test/public>
		AllowOverride all
	</Directory>

    SSLEngine on
    SSLCertificateFile /etc/apache2/ssl/cert.pem
    SSLCertificateKeyFile /etc/apache2/ssl/cert-key.pem
</VirtualHost>

<VirtualHost *:443>
    ServerAdmin robert@samarija.com
    DocumentRoot ${APACHE_DOCUMENT_ROOT}/foerderverein/public
    ServerName foerderverein.dev.local
	<Directory ${APACHE_DOCUMENT_ROOT}/foerderverein/public>
		AllowOverride all
	</Directory>

    SSLEngine on
    SSLCertificateFile /etc/apache2/ssl/cert.pem
    SSLCertificateKeyFile /etc/apache2/ssl/cert-key.pem
</VirtualHost>

<VirtualHost *:443>
    ServerAdmin robert@samarija.com
    DocumentRoot ${APACHE_DOCUMENT_ROOT}/vatra/public
    ServerName vatra.dev.local
	<Directory ${APACHE_DOCUMENT_ROOT}/vatra/public>
		AllowOverride all
	</Directory>

    SSLEngine on
    SSLCertificateFile /etc/apache2/ssl/cert.pem
    SSLCertificateKeyFile /etc/apache2/ssl/cert-key.pem
</VirtualHost>

<VirtualHost *:443>
    ServerAdmin robert@samarija.com
    DocumentRoot ${APACHE_DOCUMENT_ROOT}/voda/public
    ServerName voda.dev.local
	<Directory ${APACHE_DOCUMENT_ROOT}/voda/public>
		AllowOverride all
	</Directory>

    SSLEngine on
    SSLCertificateFile /etc/apache2/ssl/cert.pem
    SSLCertificateKeyFile /etc/apache2/ssl/cert-key.pem
</VirtualHost>

<VirtualHost *:443>
    ServerAdmin robert@samarija.com
    DocumentRoot ${APACHE_DOCUMENT_ROOT}/kamen/public
    ServerName kamen.dev.local
	<Directory ${APACHE_DOCUMENT_ROOT}/kamen/public>
		AllowOverride all
	</Directory>

    SSLEngine on
    SSLCertificateFile /etc/apache2/ssl/cert.pem
    SSLCertificateKeyFile /etc/apache2/ssl/cert-key.pem
</VirtualHost>